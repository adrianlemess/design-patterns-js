{
  "name": "bff",
  "version": "1.0.0",
  "description": "BFF created in Node",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "dev": "nodemon index.ts",
    "start": "nodemon ./dist/index.js",
    "prod": "npm run build && npm run start",
    "test": "jest",
    "test:watch": "jest --watch"
  },
  "keywords": [
    "bff",
    "nodejs",
    "graphql"
  ],
  "author": "adrianlemess",
  "license": "MIT",
  "dependencies": {
    "@types/node": "^13.1.2",
    "ts-node": "^8.10.1"
  },
  "devDependencies": {
    "@types/jest": "^24.0.25",
    "cross-env": "^5.2.0",
    "eslint": "^5.16.0",
    "husky": "^2.1.0",
    "jest": "^24.8.0",
    "nodemon": "^1.18.11",
    "ts-jest": "^24.2.0",
    "typescript": "^3.4.5"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node",
    "testPathIgnorePatterns": [
      "<rootDir>/dist/"
    ]
  }
}
